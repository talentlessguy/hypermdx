{
  "name": "basic",
  "private": true,
  "description": "",
  "type": "module",
  "scripts": {
    "build": "tsup server.ts --format=esm",
    "dev": "tsup server.ts --watch --format=esm"
  },
  "dependencies": {
    "@tinyhttp/app": "^1.3.3",
    "hyperapp": "^2.0.18",
    "hyperapp-render": "^4.0.0",
    "hypermdx": "link:../../"
  },
  "devDependencies": {
    "tsup": "^6.6.3"
  }
}
